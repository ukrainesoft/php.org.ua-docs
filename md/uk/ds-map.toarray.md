---
navigation:
  - ds-map.sum.md: '« Ds\\Map::sum'
  - ds-map.union.md: 'Ds\\Map::union »'
  - index.md: PHP Manual
  - class.ds-map.md: Ds\\Map
title: 'Ds\\Map::toArray'
origin_hash: ddf652f5224dc9f1fa9671347921941ca401ea50
---
# Ds\\Map::toArray

(PECL ds >= 1.0.0)

Ds\\Map::toArray — Перетворює колекцію на масив (array)

### Опис

```methodsynopsis
public Ds\Map::toArray(): array
```

Перетворює колекцію на масив (array).

**Застереження**

Колекції пар, що містять нескалярні ключі, не можуть бути перетворені на масив (array).

**Застереження**

Масив (array) вважатиме всі числові ключі за цілочисленні, тобто обидва ключі `"1"`и будуть сприйняті як і лише один із елементів потрапить у результуючий масив.

> **Зауваження** :
> 
> Приведення до типу array поки що не підтримується.

### Список параметрів

Ця функція не має параметрів.

### Значення, що повертаються

Повертає масив (array), що містить всі елементи колекції, зберігаючи порядок, у якому зберігалися в колекції.

### Приклади

**Приклад #1 Приклад использования метода**Ds\\Map::toArray()\*\*\*\*

```php
<?php
$map = new \Ds\Map([
    "a" => 1,
    "b" => 2,
    "c" => 3,
]);

var_dump($map->toArray());
?>
```

Висновок наведеного прикладу буде схожим на:

```
array(3) {
  ["a"]=>
  int(1)
  ["b"]=>
  int(2)
  ["c"]=>
  int(3)
}
```
