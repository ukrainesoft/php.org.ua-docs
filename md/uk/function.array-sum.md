---
navigation:
  - function.array-splice.md: « array\_splice
  - function.array-udiff-assoc.md: array\_udiff\_assoc »
  - index.md: PHP Manual
  - ref.array.md: Функції для роботи з масивами
title: array\_sum
origin_hash: ddf652f5224dc9f1fa9671347921941ca401ea50
---
# array\_sum

(PHP 4 >= 4.0.4, PHP 5, PHP 7, PHP 8)

array\_sum - Обчислює суму значень масиву

### Опис

```methodsynopsis
array_sum(array $array): int|float
```

Функция**array\_sum()** повертає суму значень масиву.

### Список параметрів

`array`

Вхідний масив

### Значення, що повертаються

Повертає суму значень у вигляді цілого числа чи числа з плаваючою точкою; , якщо масив (`array`) Порожній.

### список змін

| Версия | Опис |
| --- | --- |
| 8.3.0 | Тепер видає помилку рівня **`E_WARNING`**, коли значення масиву (`array`) неможливо перетворити на ціле число (int) або число з плаваючою точкою (float). Раніше масиви (array) та об'єкти (object) ігнорувалися, тоді як інші значення приводилися до цілої кількості (int). Більше того, об'єкти, що визначають числове наведення (наприклад, об'єкти класу [GMP](class.gmp.md)), тепер наводяться, а чи не ігноруються. |

### Приклади

**Пример #1 Пример использования функции**array\_sum()\*\*\*\*

```php
<?php

$a = array(2, 4, 6, 8);
echo "sum(a) = " . array_sum($a) . "\n";

$b = array("a" => 1.2, "b" => 2.3, "c" => 3.4);
echo "sum(b) = " . array_sum($b) . "\n";

?>
```

Результат виконання наведеного прикладу:

```
sum(a) = 20
sum(b) = 6.9
```
