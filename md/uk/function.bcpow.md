---
navigation:
  - function.bcmul.md: « bcmul
  - function.bcpowmod.md: bcpowmod »
  - index.md: PHP Manual
  - ref.bc.md: Функції BC Math
title: bcpow
origin_hash: ddf652f5224dc9f1fa9671347921941ca401ea50
---
# bcpow

(PHP 4, PHP 5, PHP 7, PHP 8)

bcpow — Зведення до ступеня чисел із довільною точністю

### Опис

```methodsynopsis
bcpow(string $num, string $exponent, ?int $scale = null): string
```

Зводить `num`в степень`exponent`

### Список параметрів

`num`

Підстава у вигляді рядка.

`exponent`

Ступінь у вигляді рядка. Якщо ступінь не ціле, вона обрізається до цілого. Коректні значення ступеня залежать від використовуваної платформи, але це як мінімум значення від `-2147483648`до`2147483647`

`scale`

Цей необов'язковий параметр використовують для налаштування кількості знаків після десяткового роздільника в результаті. Якщо не задано, то, за замовчуванням, буде використано значення, задане глобально за допомогою [bcscale()](function.bcscale.md), либо

### Значення, що повертаються

Повертає результат у вигляді рядка.

### список змін

| Версия | Опис |
| --- | --- |
| 7.3.0 | Тепер **bcpow()** повертає числа із заданою точністю. Раніше завершальні нулі в дрібній частині числа відкидалися. |

### Приклади

**Пример #1 Пример использования**bcpow()\*\*\*\*

```php
<?php

echo bcpow('4.2', '3', 2); // 74.08

?>
```

### Примітки

> **Зауваження** :
> 
> До PHP 7.3.0**bcpow()** могла повернути результат із меншим числом цифр після десяткової точки, ніж зазначено в аргументі `scale`. Так відбувалося, тільки коли результату не потрібна вся точність, надана в `scale`Например:
> 
> **Пример #2 Пример уменьшения точности в**bcpow()\*\*\*\*
> 
> ```php
> <?php
> echo bcpow('5', '2', 2); // видасть "25" замість "25.00"
> ?>
> ```

### Дивіться також

-   [bcpowmod()](function.bcpowmod.md) \- Зводить одне число у ступінь іншого та повертає залишок від поділу результату на третє число
-   [bcsqrt()](function.bcsqrt.md) \- Витягує квадратний корінь із числа із заданою точністю
