---
navigation:
  - ref.dir.md: « Функції для роботи з каталогами
  - function.chroot.md: chroot »
  - index.md: PHP Manual
  - ref.dir.md: Функції для роботи з каталогами
title: chdir
origin_hash: ddf652f5224dc9f1fa9671347921941ca401ea50
---
# chdir

(PHP 4, PHP 5, PHP 7, PHP 8)

chdir — Змінює каталог

### Опис

```methodsynopsis
chdir(string $directory): bool
```

Змінює поточний каталог PHP на вказаний як параметр `directory`

### Список параметрів

`directory`

Новий поточний каталог

### Значення, що повертаються

Повертає **`true`** у разі успішного виконання або \*\*`false`\*\*в случае возникновения ошибки.

### Помилки

У разі невдалого виконання видає помилку рівня **`E_WARNING`**

### Приклади

**Приклад #1 Приклад використання** chdir()\*\*\*\*

```php
<?php

// текущий каталог
echo getcwd() . "\n";

chdir('public_html');

// текущий каталог
echo getcwd() . "\n";

?>
```

Висновок наведеного прикладу буде схожим на:

```
/home/vincent
/home/vincent/public_html
```

### Примітки

**Застереження**

Якщо PHP-інтерпретатор зібраний за допомогою ZTS (Zend Thread Safety), будь-які зміни в поточному каталозі, зроблені за допомогою **chdir()**, не буде виявлено операційною системою. Усі вбудовані функції PHP, як і раніше, враховуватимуть зміни у поточному каталозі; це не відноситься до викликаних функцій зовнішніх бібліотек, підключених через [FFI](book.ffi.md)Используйте**php -i** або вбудовану константу **`PHP_ZTS`**, щоб дізнатися, чи зібрано PHP з ZTS.

### Дивіться також

-   [getcwd()](function.getcwd.md) \- Отримує ім'я поточного робочого каталогу
