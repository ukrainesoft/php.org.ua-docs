---
navigation:
  - function.ctype-print.md: « ctype\_print
  - function.ctype-space.md: ctype\_space »
  - index.md: PHP Manual
  - ref.ctype.md: Опції Ctype
title: ctype\_punct
origin_hash: ddf652f5224dc9f1fa9671347921941ca401ea50
---
# ctype\_punct

(PHP 4 >= 4.0.4, PHP 5, PHP 7, PHP 8)

ctype\_punct — Перевірка друкованих символів, які не містять символів пробілів або літер.

### Опис

```methodsynopsis
ctype_punct(mixed $text): bool
```

Перевіряє, чи передано рядок (string) `text` тільки із знаків пунктуації.

### Список параметрів

`text`

Перевірений рядок.

> **Зауваження** :
> 
> Якщо передано ціле число (int) в діапазоні між -128 і 255 включно, воно буде оброблено як ASCII-код одного символу (до негативних значень буде додано 256, щоб функція могла представити символи з розширеного діапазону ASCII). Інші цілі числа будуть оброблені як рядки, що містять десяткові цифри цілих чисел.

**Увага**

Починаючи з PHP 8.1.0, передача нерядкових аргументів застаріла. У майбутньому аргумент замість ASCII-коду інтерпретуватиметься як рядок. Залежно від передбачуваної поведінки аргумент або перетворюють у рядок (string), або викликають функцію [chr()](function.chr.md)

### Значення, що повертаються

Повертає **`true`**, якщо кожен символ у рядку `text` — друкований, але не буква, не цифра чи пропуск, інакше **`false`**. При виклику з порожнім рядком результатом завжди буде **`false`**

### Приклади

**Приклад #1 Приклад використання функції** ctype\_punct()\*\*\*\*

```php
<?php

$strings = array('ABasdk!@!$#', '!@ # $', '*&$()');
foreach ($strings as $testcase) {
    if (ctype_punct($testcase)) {
        echo "Строка $testcase состоит только из знаков пунктуации.\n";
    } else {
        echo "Строка $testcase не состоит только из знаков пунктуации.\n";
    }
}
?>
```

Результат виконання наведеного прикладу:

```
Строка ABasdk!@!$# не состоит только из знаков пунктуации.
Строка !@ # $ не состоит только из знаков пунктуации.
Строка *&$() состоит только из знаков пунктуации.
```

### Дивіться також

-   [ctype\_cntrl()](function.ctype-cntrl.md) \- Перевіряє керуючі символи
-   [ctype\_graph()](function.ctype-graph.md) \- Перевіряє будь-які друковані символи крім пробілу
-   [IntlChar::ispunct()](intlchar.ispunct.md) \- Перевіряє, чи символ символом пунктуації
