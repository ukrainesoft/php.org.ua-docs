Малює дугу за годинниковою стрілкою

-   [«psarc](function.ps-arc.html)
    
-   [псbeginpage »](function.ps-begin-page.html)
    
-   [PHP Manual](index.html)
    
-   [Функції PS](ref.ps.html)
    
-   Малює дугу за годинниковою стрілкою
    

# псarcn

(PECL ps >= 1.1.0)

псarcn — Малює дугу за годинниковою стрілкою

### Опис

```methodsynopsis
ps_arcn(    resource $psdoc,    float $x,    float $y,    float $radius,    float $alpha,    float $beta): bool
```

Малює частину кола із середньою точкою в точці (`x` `y`). Дуга починається під кутом `alpha` і закінчується під кутом `beta`. Вона малюється за годинниковою стрілкою (використовуйте [псarc()](function.ps-arc.html) для малювання проти годинникової стрілки). Дочірній шлях, доданий до поточного шляху, починається на дузі під кутом `alpha` і закінчується на дузі під кутом `beta`

### Список параметрів

`psdoc`

Ідентифікатор ресурсу файлу postscript, повернутий функцією [псnew()](function.ps-new.html)

`x`

Координата X середньої точки кола.

`y`

Координата Y середньої точки кола.

`radius`

Радіус кола.

`alpha`

Початковий кут у градусах.

`beta`

Кінцевий кут у градусах.

### Значення, що повертаються

Повертає **`true`** у разі успішного виконання або **`false`** у разі виникнення помилки.

### Дивіться також

-   [псarc()](function.ps-arc.html) - Малює дугу проти годинникової стрілки