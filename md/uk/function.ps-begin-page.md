---
navigation:
  - function.ps-arcn.md: « ps\_arcn
  - function.ps-begin-pattern.md: ps\_begin\_pattern »
  - index.md: PHP Manual
  - ref.ps.md: Функції PS
title: ps\_begin\_page
origin_hash: ddf652f5224dc9f1fa9671347921941ca401ea50
---
# ps\_begin\_page

(PECL ps >= 1.1.0)

ps\_begin\_page — Починає нову сторінку

### Опис

```methodsynopsis
ps_begin_page(resource $psdoc, float $width, float $height): bool
```

Починає нову сторінку. Хоча параметри `width`и`height` мають на увазі різний розмір сторінки для кожної сторінки, це неможливо в PostScript. Перший виклик **ps\_begin\_page()** встановлює розмір сторінки для документа. Наступні дзвінки не матимуть жодного ефекту, окрім створення нової сторінки. Ситуація інша, якщо ви збираєтеся перетворити документ PostScript на PDF. Функція містить у документі мітки PDF, які можуть індивідуально встановлювати розмір кожної сторінки. Отриманий PDF-документ матиме різні розміри сторінок.

Хоча PostScript не знає різних розмірів сторінок, pslib поміщає документ обмежувальну рамку кожної сторінки. Цей розмір оцінюється деякими програмами перегляду PostScript та має пріоритет над BoundingBox у заголовку документа. Це може призвести до несподіваних результатів, якщо ви встановите BoundingBox, нижній лівий кут якого не дорівнює (0, 0), тому що обмежує рамку сторінки завжди буде нижній лівий кут (0, 0) і перезапише глобальне налаштування.

Кожна сторінка інкапсулюється у режим збереження/відновлення. Це означає, що більшість налаштувань, зроблених на одній сторінці, не буде збережено на наступній сторінці.

Якщо до першого виклику **ps\_begin\_page()** не було виклику [ps\_findfont()](function.ps-findfont.md), то буде виведено заголовок документа PostScript, а розмір рамки, що обмежує, буде встановлений на першу сторінку. Нижній лівий кут обмежувальної рамки встановлено (0, 0). Якщо [ps\_findfont()](function.ps-findfont.md) була викликана раніше, отже, заголовок вже було виведено і документ не матиме коректної обмежувальної рамки. Щоб запобігти цьому, слід викликати [ps\_set\_info()](function.ps-set-info.md), щоб встановити інформаційне поле `BoundingBox` і, можливо, `Orientation` перед будь-яким викликом [ps\_findfont()](function.ps-findfont.md)или**ps\_begin\_page()**

> **Зауваження** :
> 
> До версії 0.2.6 pslib функція завжди перезаписуватиме BoundingBox і Orientation, якщо вони були встановлені раніше за допомогою [ps\_set\_info()](function.ps-set-info.md) і [ps\_findfont()](function.ps-findfont.md) раніше не викликалася.

### Список параметрів

`psdoc`

Ідентифікатор ресурсу файлу postscript, повернутий функцією [ps\_new()](function.ps-new.md)

`width`

Ширина сторінки в пікселях, наприклад 596 для формату А4.

`height`

Висота сторінки в пікселях, наприклад 842 для формату А4.

### Значення, що повертаються

Повертає **`true`** у разі успішного виконання або \*\*`false`\*\*в случае возникновения ошибки.

### Дивіться також

-   [ps\_end\_page()](function.ps-end-page.md) \- Завершує сторінку
-   [ps\_findfont()](function.ps-findfont.md) \- Завантажує шрифт
-   [ps\_set\_info()](function.ps-set-info.md) \- встановлює інформаційні поля документа
