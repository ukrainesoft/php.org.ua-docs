---
navigation:
  - ref.sodium.md: « Функції Sodium
  - function.sodium-base642bin.md: sodium\_base642bin »
  - index.md: PHP Manual
  - ref.sodium.md: Опції Sodium
title: sodium\_add
origin_hash: ddf652f5224dc9f1fa9671347921941ca401ea50
---
# sodium\_add

(PHP 7 >= 7.2.0, PHP 8)

sodium\_add - Додає великі числа

### Опис

```methodsynopsis
sodium_add(string &$string1, string $string2): void
```

Добавляет параметр`string2`к`string1`, перезаписуючи значення, що зберігається в `string1`. Функція передбачає, що обидва параметри є двійковими рядками, які представляють цілі числа без знака у порядку байтів.

### Список параметрів

`string1`

Рядок, що представляє ціле число довільної довжини без знака у зворотному порядку байтів. Цей параметр передається за посиланням та міститиме суму двох параметрів.

`string2`

Рядок, що представляє ціле число довільної довжини без знака у зворотному порядку байтів.

### Значення, що повертаються

Функція не повертає значення після виконання.
