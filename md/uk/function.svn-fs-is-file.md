Визначає, чи знаходиться файл по даному шляху

-   [« svnфсісdir](function.svn-fs-is-dir.html)
    
-   [svnфсmakedir »](function.svn-fs-make-dir.html)
    
-   [PHP Manual](index.html)
    
-   [Функції SVN](ref.svn.html)
    
-   Визначає, чи знаходиться файл по даному шляху
    

# svnфсісfile

(PECL svn >= 0.2.0)

svnфсісfile — Визначає, чи знаходиться файл по даному шляху.

### Опис

```methodsynopsis
svn_fs_is_file(resource $root, string $path): bool
```

**Увага**

На цей час ця функція ще була документована; для ознайомлення доступний лише перелік аргументів.

Визначає, чи знаходиться файл по даному шляху.

### Список параметрів

`root`

`path`

### Значення, що повертаються

Повертає **`true`**, якщо по даному шляху знаходиться файл, інакше повертає **`false`**

### Примітки

**Увага**

Ця функція є *ЕКСПЕРИМЕНТАЛЬНОЇ*. Поведінка цієї функції, її ім'я та документація, що до неї належить, можуть змінитися в наступних версіях PHP без повідомлення. Використовуйте цю функцію на свій страх та ризик.