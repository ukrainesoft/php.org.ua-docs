Оператор управління помилками

-   [« Оператори порівняння](language.operators.comparison.html)
    
-   [Операторы исполнения »](language.operators.execution.html)
    
-   [PHP Manual](index.html)
    
-   [Оператори](language.operators.html)
    
-   Оператор управління помилками
    

## Оператор управління помилками

PHP підтримує один оператор керування помилками: знак `@`. У випадку, якщо він передує будь-якому виразу в PHP-коді, будь-які повідомлення про помилки, що генеруються цим виразом, будуть пригнічені.

Якщо користувальницьку функцію обробника помилок встановлено за допомогою [seterrorhandler()](function.set-error-handler.html), вона все одно буде викликатись, навіть якщо діагностика була пригнічена.

**Увага**

До версії PHP 8.0.0 функція [errorreporting()](function.error-reporting.html), що викликається всередині оброблювача помилок, завжди повертала `0`якщо помилка була пригнічена оператором `@`. Починаючи з PHP 8.0.0, вона повертає значення `E_ERROR | E_CORE_ERROR | E_COMPILE_ERROR | E_USER_ERROR | E_RECOVERABLE_ERROR | E_PARSE`

Будь-яке повідомлення про помилку, згенероване виразом, доступне в елементі масиву `"message"`, що повертається [errorgetlast()](function.error-get-last.html). Результат цієї функції змінюватиметься при кожній помилці, тому його необхідно перевірити заздалегідь.

```php
<?php
// Преднамеренная ошибка при работе с файлами
$my_file = @file ('non_existent_file') or
    die ("Ошибка при открытии файла: сообщение об ошибке было таким: '" . error_get_last()['message'] . "'");

// работает для любых выражений, а не только для функций
$value = @$cache[$key];
// В случае если ключа $key нет, сообщение об ошибке (notice) не будет отображено

?>
```

> **Зауваження**: Оператор `@` працює тільки з [виразами](language.expressions.html). Є просте правило: якщо щось повертає значення, то ви можете використовувати перед ним оператор `@`. Наприклад, ви можете використовувати `@` перед іменем змінної, довільною функцією або викликом [include](function.include.html) і так далі. У той же час ви не можете використовувати цей оператор перед визначенням функції або класу умовними конструкціями, такими як `if` [foreach](control-structures.foreach.html) і т.д.

**Увага**

До PHP 8.0.0 оператор `@` міг пригнічувати критичні помилки, які переривали виконання скрипту. Наприклад, додавання `@` до виклику неіснуючої функції, якщо вона недоступна або написана неправильно, подальша робота скрипта призведе до переривання виконання скрипта без будь-яких повідомлень.

### Дивіться також

-   [errorreporting()](function.error-reporting.html)
-   [Обробка помилок та функції логування](ref.errorfunc.html)