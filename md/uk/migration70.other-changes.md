---
navigation:
  - migration70.removed-exts-sapis.md: « Віддалені модулі та SAPI
  - migration56.md: Миграция с PHP 5.5.x на PHP 5.6.x »
  - index.md: PHP Manual
  - migration70.md: Миграция с PHP 5.6.x на PHP 7.0.x
title: Інші зміни
---
## Інші зміни

### Послаблення обмежень використання зарезервованих слів

Глобально зарезервовані слова можна використовувати для властивостей, констант і методів у класах, інтерфейсах і трейтах. Це зменшує масштаби проблем зворотної сумісності під час введення нових ключових слів і дозволяє уникнути обмежень для API.

Це особливо корисно при створенні внутрішніх DSL з текучими інтерфейсами:

```php
<?php
// Ранее 'new', 'private' и 'for' использовать было нельзя.
Project::new('Project Name')->private()->for('purpose here')->with('username here');
?>
```

Єдине обмеження, що слово `class` все ще не можна використовувати як ім'я константи, так як воно буде перетинатися із синтаксисом дозволу імені класу (`ClassName::class`

### Видалення попередження date.timezone

Раніше викликалося попередження, якщо INI-налаштування `date.timezone` не було встановлено раніше використання будь-яких функцій роботи з датою або часом. Тепер це попередження видалено (але `date.timezone` за замовчуванням все ще UTC).
