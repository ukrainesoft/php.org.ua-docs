---
navigation:
  - tutorial.firstpage.html: « Перша сторінка на PHP
  - tutorial.forms.html: Работа с формами »
  - index.html: PHP Manual
  - tutorial.html: Простий підручник
title: Робимо щось корисне
---
# Робимо щось корисне

Давайте зробимо щось корисне. Наприклад, визначимо, який браузер використовує той, хто дивиться зараз нашу сторінку. Для цього ми перевіримо рядок з ім'ям браузера, що надсилається нам у HTTP-запиті. Ця інформація зберігається в [переменной](language.variables.html). Змінні в PHP завжди передуються знаком долара. Що цікавить нас в даний момент змінна називається [SERVER\['HTTPUSERAGENT'\]](reserved.variables.server.html)

> **Зауваження**
> 
> [SERVER](reserved.variables.server.html) - Спеціальна зарезервована змінна PHP, яка містить всю інформацію, отриману від Web-сервера. Її також називають суперглобальною. Для більш детальної інформації дивіться розділ [Суперглобальні змінні](language.variables.superglobals.html)

Для виведення цієї змінної ми зробимо так:

**Приклад #1 Виведення значення змінної (елемента масиву)**

```php
<?php
echo $_SERVER['HTTP_USER_AGENT'];
?>
```

Приклад виведення цієї програми:

Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)

У PHP існує величезна кількість [типов](language.types.html) змінних. У попередньому прикладі ми друкували елемент [масиву](language.types.array.html). Масиви можуть бути дуже корисними.

[SERVER](reserved.variables.server.html) - це просто одна із змінних, які надаються вам мовою PHP. Список таких змінних можна переглянути у розділі "[Зарезервовані змінні](reserved.variables.html)" або переглянувши висновок функції [phpinfo()](function.phpinfo.html), що використовується в прикладі попереднього розділу.

Всередині PHP-тегів можна поміщати кілька виразів і створювати маленькі блоки коду, що роблять більше, ніж простий виклик echo. Наприклад, якщо ви хочете додати перевірку для Internet Explorer, можна зробити так:

**Приклад #2 Приклад використання [управляющих структур](language.control-structures.html) і [функций](language.functions.html)**

```php
<?php
if (strpos($_SERVER['HTTP_USER_AGENT'], 'MSIE') !== FALSE) {
    echo 'Вы используете Internet Explorer.<br />';
}
?>
```

Приклад виведення цієї програми:

```
Вы используете Internet Explorer.<br />
```

Тут ми показали кілька нових елементів. По-перше, тут є конструкція [іф](control-structures.if.html). Якщо ви знайомі з основами синтаксису мови C, ви вже помітили щось схоже. Якщо ж ви не знаєте C або подібної по синтаксису мови, то найкращий варіант - взяти якусь вступну книжку по PHP і прочитати перші пару розділів. Інший варіант - почитати розділ [Опис мови](langref.html) цього керівництва.

Крім цього, тут є виклик функції [strpos()](function.strpos.html). . [strpos()](function.strpos.html) - вбудована в PHP функція, яка шукає один рядок на інший. У даному випадку ми шукаємо рядок `'MSIE'` (так звану "голку" - needle) [SERVER\['HTTPUSERAGENT'\]](reserved.variables.server.html) (У так званому "сіні" - haystack). Якщо "голка" знайдена всередині "сіна", функція повертає позицію "голки" щодо початку "сіна". Інакше вона повертає **`false`**. Якщо вона не поверне **`false`**, то умова в [іф](control-structures.if.html) виявиться істинним (**`true`**), і код у фігурних дужках ({ }) виконається. В іншому випадку код не виконується. Спробуйте створити аналогічні приклади з використанням команд [іф](control-structures.if.html) [else](control-structures.else.html) та інших функцій, таких, як [strtoupper()](function.strtoupper.html) і [strlen()](function.strlen.html). Також подібні приклади містяться у багатьох описах функцій у цьому посібнику. Якщо ви не знаєте, як використовувати функції, можливо, вам варто прочитати сторінку посібника про те, [як читати визначення функцій](about.prototypes.html) та розділ про [функциях в PHP](language.functions.html)

Продемонструємо, як можна входити в режим PHP коду і виходити з нього навіть прямо посередині блоку з кодом:

**Приклад #3 Змішування режимів HTML та PHP**

```php
<?php
if (strpos($_SERVER['HTTP_USER_AGENT'], 'MSIE') !== FALSE) {
?>
<h3>strpos(), должно быть, вернул не false</h3>
<p>Вы используете Internet Explorer</p>
<?php
} else {
?>
<h3>strpos() вернул false</h3>
<p>Вы не используете Internet Explorer</p>
<?php
}
?>
```

Приклад виведення цієї програми:

```
<h3>strpos(), должно быть, вернул не false</h3>
<p>Вы используете Internet Explorer</p>
```

Замість використання команди PHP echo для виведення ми вийшли з режиму коду і просто послали вміст HTML. Важливий момент тут те, що логічна структура PHP коду при цьому не втрачається. Тільки одна HTML-частина буде надіслана клієнту залежно від результату функції [strpos()](function.strpos.html) (іншими словами, залежно від того, знайдено був рядок `MSIE` чи ні).
